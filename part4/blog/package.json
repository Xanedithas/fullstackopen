{
  "name": "blog",
  "version": "0.0.1",
  "description": "Blog API",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon -r dotenv/config index.js",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand",
    "test:api": "npm run test -- tests/blog_api.test.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-env": "^7.0.2",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "mongodb": "^3.6.1",
    "mongoose": "^5.10.2"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "eslint": "^7.9.0",
    "jest": "^26.4.2",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
